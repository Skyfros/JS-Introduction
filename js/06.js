/* Math Object */

let resultado;

resultado = Math.PI; // Pi (aproximadamente 3.1415
resultado = Math.round(2.5); // Redondea al más cercano
resultado = Math.ceil(2.1); // Redondea al entero superior
resultado = Math.floor(2.1); // Redondea al entero inferior 
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // Valor absoluto
resultado = Math.min(3, 5, 1, 12, -3); // Mínimo
resultado = Math.max(3, 5, 1, 12, -3);
resultado = Math.random(); // Número aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 100) + 1; // Número aleatorio


console.log(resultado);